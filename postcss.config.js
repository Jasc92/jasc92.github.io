/**
 * PostCSS Configuration
 * 
 * Configures Tailwind CSS v4 with the @tailwindcss/postcss plugin.
 * This enables utility-first CSS generation during the build process.
 */
export default {
    plugins: {
        '@tailwindcss/postcss': {}
    }
};
